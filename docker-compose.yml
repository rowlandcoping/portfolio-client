services:
  client:
    build: .
    container_name: portfolio-client
    restart: unless-stopped
    ports:
      - "5173:5173"
    volumes:
      - ./:/app
      - /app/node_modules
    networks:
      - shared-network

networks:
  shared-network:
    external: true